{
  "name": "shopnex-root",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true, force: true})\""
  }
}
